#This file is generated by updateDaemonJvm
# Gradle 8.13 runs on Java 17-23 only. Android Studio ships a Java 25 JBR, so pin the daemon
# to a locally installed JDK 17 regardless of which JVM launches the wrapper.
toolchainVersion=17
