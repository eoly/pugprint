#This file is generated by updateDaemonJvm
# The project targets Java 17 (AGP 9 minimum). Android Studio ships a Java 25 JBR, so pin the
# daemon to a locally installed JDK 17 regardless of which JVM launches the wrapper.
toolchainVersion=17
