Fixtures from hypotheses that were REFUTED on hardware (kept only as a record):
- cat-printer/: 51 78 framed commands (ae30 family). Wrong printer family entirely.
- escpos-guesses/: ESC * column bit-image, GS v 0 without density, text. Printed nothing or partial.
Do NOT use these for :core:printer tests. The real fixtures are ../print_job/*.vendor.hex.
