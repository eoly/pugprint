# Step 5 — HCI snoop capture of the official app

Capture file: `captures/`
Phone / Android version:
Image printed (describe or link the source bitmap):

## Wireshark
Filter used (e.g. `btatt.opcode == 0x52 && btatt.handle == 0x____`):
ae01 handle:
ae02 handle:

## Observed write sequence (hex, in order)
```
```

## Timing
- Bytes per write:
- Typical gap between writes:
- Gap before first row / after last row:

## Notifications received (hex)
```
```

## Diff vs `print_test.py`
| Field / step | Official app | Our script | Fix applied |
|--------------|--------------|------------|-------------|
